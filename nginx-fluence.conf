# Default server — catch everything that doesn't match a server_name
# Return 444 (close connection without response) to prevent IP exposure
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
